[project]
name = "snake-lang"
version = "1.0.0"
description = "The Snake programming language"
requires-python = ">=3.10"

[project.scripts]
snake = "snake.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
